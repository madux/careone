<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_inherit_pharmacy" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.pharmacy</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                 <field name="patient_no"/>
            </xpath>
            <xpath expr="//page[@name='internal_notes']" position="after">
                <page string="Pharmacy" invisible="is_company == True">
                    <group>
                        <group>
                            <field name="prescription_count"/>
                            <field name="last_visit_date"/>
                        </group>
                    </group>
                    <group>
                        <field name="allergy_ids" widget="many2many_tags"/>
                    </group>
                    <group>
                        <field name="chronic_condition_ids" widget="many2many_tags"/>
                    </group>
                    <separator string="Pharmacy History"/>
                    <field name="pharmacy_history_ids">
                        <tree>
                            <field name="name"/>
                            <field name="date"/>
                            <field name="branch_id"/>
                            <field name="stage_id"/>
                            <field name="state"/>
                            <field name="total_amount"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>